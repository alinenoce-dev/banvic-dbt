 models:
      - name: dim_clientes
        description: Modelo dimensão clientes.
        columns:
          - name: pk_cliente
            description: chave primária do cliente.
            data_tests:
              - unique
              - not_null